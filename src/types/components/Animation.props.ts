import { ViewProps } from './View.props'

export interface AnimatedWrapperProps extends ViewProps {
  delay?: number
  duration?: number
}

export interface AnimationComponentProps {
  delay?: number
}
